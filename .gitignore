# Astro build output and cache
dist/
.astro/

# Dependencies
node_modules/

# Environment variables (never commit secrets/API keys)
.env
.env.production

# Editor/OS files
.DS_Store

# Local-only Claude Code settings (machine-specific, not meant to be shared)
.claude/settings.local.json
